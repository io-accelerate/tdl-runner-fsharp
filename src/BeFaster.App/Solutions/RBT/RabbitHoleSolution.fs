namespace BeFaster.App.Solutions.RBT

open System
open System.Collections.Generic

type RabbitHoleSolution() =
    member _.RabbitHole (rows: int, columns: int, diggingMoves: string, renderingOptions: Dictionary<string, string>) : string =
        raise (NotImplementedException())
