namespace BeFaster.App.Solutions.DMO

open System
open System.Collections.Generic

type DemoRound2Solution() =
    member _.ArraySum(arr: List<int>): int =
        raise (NotImplementedException())

    member _.IntRange(start: int, finish: int): List<int> =
        raise (NotImplementedException())

    member _.FilterPass(arr: List<int>, threshold: int): List<int> =
        raise (NotImplementedException())
