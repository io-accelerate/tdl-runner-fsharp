namespace BeFaster.App.Solutions.DMO

open System

type DemoRound1Solution() =
    member _.Increment(x: int): int =
        raise (NotImplementedException())

    member _.ToUppercase(s: string): string =
        raise (NotImplementedException())

    member _.LetterToSanta(): string =
        raise (NotImplementedException())

    member _.CountLines(s: string): int =
        raise (NotImplementedException())
