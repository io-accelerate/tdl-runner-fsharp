namespace BeFaster.App.Solutions.AMZ

open System
open System.Collections.Generic

type AmazingSolution() =
    member _.AmazingMaze (rows: int, columns: int, mazeGenerationOptions: Dictionary<string, string>) : string =
        raise (NotImplementedException())
